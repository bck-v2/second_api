class TestApp:
    def test_home(self, testapp) -> None:
        res = testapp.get("/", status=200)
        assert b"<h1>caseinterview</h1>" in res.body

class TestApp:
    def test_home(self, testapp) -> None:
        res = testapp.get("/", status=200)
        assert b"<h1>caseinterview</h1>" in res.body

class TestApp:
    def test_home(self, testapp) -> None:
        res = testapp.get("/", status=200)
        assert b"<h1>caseinterview</h1>" in res.body

class TestApp:
    def test_home(self, testapp) -> None:
        res = testapp.get("/", status=200)
        assert b"<h1>caseinterview</h1>" in res.body

class TestApp:
    def test_home(self, testapp) -> None:
        res = testapp.get("/", status=200)
        assert b"<h1>caseinterview</h1>" in res.body

class TestApp:
    def test_home(self, testapp) -> None:
        res = testapp.get("/", status=200)
        assert b"<h1>caseinterview</h1>" in res.body

class TestApp:
    def test_home(self, testapp) -> None:
        res = testapp.get("/", status=200)
        assert b"<h1>caseinterview</h1>" in res.body